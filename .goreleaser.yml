# .goreleaser.yml
project_name: command-count

build:
  main: cmd/command-count/main.go
  binary: cmc
  goos:
    - darwin
    - linux
  goarch:
    - amd64
archive:
  format: tar.gz
  replacements:
    amd64: 64-bit
    darwin: macOS
    linux: Tux
